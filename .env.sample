# Frontend environment variables
# Copy this file to .env (or .env.local for machine-specific overrides) and fill in the values.

# API server base URL
VUE_APP_API_HOST=http://localhost:3000

# Google OAuth client configuration
VUE_APP_GOOGLE_CLIENT_ID=435297662121-i9miojgs1avtqt6djtt8jij3st4j92u5.apps.googleusercontent.com
# Frontend route that handles the Google OAuth redirect (update when deploying)
VUE_APP_GOOGLE_REDIRECT_URI=http://localhost:8080/auth/google/callback
